import React from "react";
import './styles.scss'

const Header = (props) =>{

    return(
        <div className="header-container1">
            <p className="font-medium-18 logged-in-user">Logged in as: Kashif</p>
        </div>
    )
}

export default Header;